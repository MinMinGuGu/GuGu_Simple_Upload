spring:
  application:
    name: GuGu_Simple_Upload
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 100GB
      max-request-size: 100GB
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://${config.db-server}/${spring.application.name}?serverTimezone=GMT&allowMultiQueries=true
    username: ENC(MBwbLOJPafjWeQQjMZVpuc4l3Uz2Ynem)
    password: ENC(ZhV4/8thHCTUYZKcWXw+0ognhJs/BbXF)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
mybatis-plus:
  global-config:
    db-config:
      logic-delete-value: 1
      logic-not-delete-value: 0
server:
  port: 80
logging:
  level:
    com.gugu.upload: debug
config:
  dir: ${user.home}/${spring.application.name}
  tmp-dir: ${config.dir}/tmp/
  log-dir: ${config.dir}/log/
  db-server: rm-wz93d1vexp93w779efo.mysql.rds.aliyuncs.com